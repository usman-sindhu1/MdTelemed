/** Static support channels (not from API). Change here or via env later. */
export const CONTACT_SUPPORT_EMAIL = 'support@mdtelemed.com';
export const CONTACT_SUPPORT_PHONE_DISPLAY = '+1 (800) 555-1234';
/** E.164-style for `tel:` / SMS links — digits after country code. */
export const CONTACT_SUPPORT_PHONE_TEL = '+18005551234';
