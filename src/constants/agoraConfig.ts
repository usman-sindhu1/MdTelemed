/**
 * Agora App ID.
 *
 * Set this to your Agora project App ID from the Agora Console.
 * Required for `react-native-agora` engine initialization.
 */
export const AGORA_APP_ID = 'f12cbf19539e48c3996fad9706e1538e';
// NOTE: Agora App ID is public (not a secret). If you need per-env values,
// consider returning `appId` from `POST /api/agora/token` and removing this constant.

